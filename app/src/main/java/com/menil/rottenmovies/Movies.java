package com.menil.rottenmovies;

import java.util.List;

/**
 * Created by menil on 17.10.2014.
 */
public class Movies {

    public List<Movie> movies;

    public void sortMoviesAscending(List<Movie> listMovies) {

      /*  for (int i = 0; i < listMovies.size(); i++) {
            int currentDate = Integer.parseInt(listMovies.get(i).release_dates.theater);
            for (int j = i; j < listMovies.size(); j++) {
                int tempDate = Integer.parseInt(listMovies.get(j).release_dates.theater);
                if (currentDate > tempDate) {
                    Movie tempMovieJ= listMovies.get(j);
                    Movie tempMovie = listMovies.get(i);
                   // listMovies.get(i).set(tempMovieJ);
                    //listMovies.get(j)=tempMovie;
                    listMovies.get(i).se
                    int temp = currentDate;
                    currentDate = tempDate;
                    tempDate = temp;
                }
            }

        }*/
    }
}
